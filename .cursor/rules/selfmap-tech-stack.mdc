---
description: SelfMap 技术栈与工程实现约束
alwaysApply: true
---

# SelfMap 技术实现规则

- 前端默认使用 Nuxt 3 + TypeScript，优先组合式 API 与可复用 composables。
- 全局状态统一使用 Pinia；禁止在多个页面重复维护同一份用户核心状态。
- 可视化默认使用 ECharts，图表配置需可复用并与业务数据解耦。
- 样式优先 TailwindCSS；遵循移动端优先并兼容桌面端。
- 页面与组件命名应语义化，避免 `test.vue`、`temp.ts` 等临时命名进入主分支。
- 任何新增依赖需说明必要性，优先轻量、维护活跃、与 Nuxt 3 兼容的方案。

# 代码质量底线

- TypeScript 不允许新增 `any`（必要时使用明确类型或泛型约束）。
- 异步请求必须处理加载态、错误态与空态。
- PR/变更说明需包含：改动目的、影响范围、验证方式。
